module.exports = require('./lib/restler');

var ipAmazon = "http://52.38.176.160/ServerEnvioLibre/rest";

var rest = require('./restler');

rest.get(ipAmazon + '/infotablero/hora?tipo=sinHora&zona=arg').on('complete', function(data) {
  console.log(data[0].message); // auto convert to object
});